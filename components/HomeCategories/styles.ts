import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
   
    image:{
        width: 150,
        height: 250,
        resizeMode:'cover',
        borderRadius: 5,
        margin:10,
    },
    title:{
        fontSize:24,
        fontWeight:'bold',
        marginTop:10,
    }
});

export default styles